import { Typography } from '@mui/material';
import React, { FC } from 'react';

const Login: FC = () => {
  return <Typography variant={'h2'}>Here is the login page!</Typography>;
};

export default Login;

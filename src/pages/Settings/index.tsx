import Container from '@mui/material/Container';
import Paper from '@mui/material/Paper';
import React, { FC } from 'react';

const Settings: FC = () => {
  return (
    <Container maxWidth="sm">
      <Paper elevation={9}>tgg</Paper>
    </Container>
  );
};

export default Settings;

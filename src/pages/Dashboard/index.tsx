import { Typography } from '@mui/material';
import React, { FC } from 'react';

const Dashboard: FC = () => {
  return (
    <>
      <Typography variant={'h2'}>Here is the dashboard!</Typography>
    </>
  );
};

export default Dashboard;
